# Copy to .env and fill in real values before deploying.
ENVIRONMENT=development   # set to "production" in prod deployments
DATABASE_URL=postgresql://argus:argus_password@localhost:5432/argus_db
SECRET_KEY=change-me-to-a-long-random-string   # REQUIRED unique value in production
DEMO_SEED_DATA=False
CHROMADB_HOST=localhost
CHROMADB_PORT=8001
LLM_PROVIDER=gemini
GEMINI_API_KEY=
OPENAI_API_KEY=
